#include <stdio.h>
/**
 * main -print putchar
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
        int n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);

		if (c != '9' )
		{
			putchar(',');
			putchar(',');
		}
	}putchar('\n');
	return (0);
