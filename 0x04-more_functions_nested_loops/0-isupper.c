#include "main.h"

/**
 * print_alphabet - a function that prints the alphabet, in lowercase
 * Return: Always 0 (Succes)
 */

int _isupper(int c)
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (1);
	}

	return (0);
}
